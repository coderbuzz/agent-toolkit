---
description: "Build with TDD as the default discipline: red-green-refactor folded into the build loop instead of a separate gate phase. Includes proportionate testing, minimal-diff rules, and dependency audit hooks."
---

Load and run the `implement` skill and follow its procedure for this task. Keep the skill's stop conditions and outputs as the source of truth.
